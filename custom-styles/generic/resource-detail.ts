import { createStyles } from "@material-ui/core";

export const styles = createStyles({

  container: {
    
  },

  iconContainer: {
    marginTop: 10,
    marginBottom: 10,
    display: "flex",
    flexDirection: "row"
  },

  iconText: {
    marginLeft: 10
  }

});
